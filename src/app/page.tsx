import { ButtonSubmit } from "./components/form";

export default function Home() {
  return (
    <main style={{ padding: 16, display: 'flex', flexDirection: 'column', gap: 4 }}>
      <span>hello</span>
      <ButtonSubmit />
    </main>
  );
}
